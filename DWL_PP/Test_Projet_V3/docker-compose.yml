version: '3.9'

services:
  application:
    build: ./app
    ports:
      - 4000:4000
  
  pageweb:
    image: php:apache
    volumes:
      - ./siteweb:/var/www/html
    ports:
      - 8000:80
    depends_on:
      - application